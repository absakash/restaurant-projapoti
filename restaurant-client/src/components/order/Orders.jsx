import React from 'react';

const Orders = () => {
      console.log("why")
      return (
            <div>
                  this is the orders
            </div>
      );
};

export default Orders;